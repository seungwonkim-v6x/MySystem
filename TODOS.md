# TODOs — Pi-first

Deferred work for the active Pi environment. Legacy Claude/Codex TODOs remain recoverable in Git history.

- [ ] Add an OS/container launch profile for untrusted repositories.
- [ ] Add a small Pi session-retention command that archives old JSONL sessions without deleting them silently.
- [ ] Measure real provider cost/cache behavior across `gpt-5.6-luna` thinking levels before changing the default again.
- [ ] Add a focused extension test harness if the Pi safety extension grows beyond one file.
