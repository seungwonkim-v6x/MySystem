# MySystem - Codex Adapter

This is the generated native projection of MySystem's canonical working agreement. Follow every block below as global user instructions. Mechanical mappings only: "Claude Code" means Codex, Anthropic policy means OpenAI policy, `CLAUDE.md` means this projection, and `.claude` paths name the canonical checkout. Codex system/developer instructions remain higher priority.

Never weaken the safety rails or the evidence-before-completion standard. Replace provider-specific tool names only with the closest Codex capability while preserving safety and observable outcomes; there are no mandatory workflow steps to preserve (ADR-0015) — skills are on-demand tools.

Keep runtime state separate. For canonical recall, read matching `~/.claude/projects/<project>/memory/MEMORY.md` and sibling notes when present; never copy Claude sessions or auth.
